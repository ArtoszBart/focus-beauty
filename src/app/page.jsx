import { Inter } from 'next/font/google';

const inter = Inter({ subsets: ['latin'] });

export const metadata = {
	title: 'Focus Beauty | strona domowa',
};

export default function Home() {
	return <main></main>;
}
